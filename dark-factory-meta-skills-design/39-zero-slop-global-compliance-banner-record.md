> **NO AI SLOP ALLOWED AT ALL - ZERO TOLERANCE TO SLOP AND HALLUCINATIONS**
> Strict compliance is mandatory. Every claim must be project-specific, evidence-backed, trace-linked, reviewer-challenged, and free of unsupported invention. If evidence is missing, mark it as an assumption, risk, open question, or failure.

# 39. Zero-Slop Global Compliance Banner Record

Status: installed.

Date: 2026-04-25.

Purpose: make the zero-slop rule visible at the top of every DFMS skill and artifact template surface.

## Required Banner

```text
NO AI SLOP ALLOWED AT ALL - ZERO TOLERANCE TO SLOP AND HALLUCINATIONS
```

## Compliance Interpretation

The banner means:

- no generic filler,
- no unsupported standards claims,
- no invented evidence,
- no fictional example treated as proof,
- no hidden assumption,
- no missing trace,
- no pass without critic review,
- no pass without fix evidence,
- no pass before required adversarial RALPH loops,
- no token-saving shortcut that weakens assurance.

## Installation Scope

Applied to:

- live installed DFMS `SKILL.md` files,
- workspace `codex-skills` DFMS `SKILL.md` files,
- generated artifact-template library Markdown files,
- generated artifact-template library review-panel starter JSON files,
- generated artifact-rubric library Markdown and JSON files,
- lower-level DFMS JSON/YAML template records,
- workspace meta-template YAML records.

## Generator Hardening

The artifact template generator and artifact rubric generator now emit the zero-slop banner/policy automatically so regeneration preserves the rule.

## Verdict

The zero-slop policy is now a top-level compliance rule, not a conversational preference.

